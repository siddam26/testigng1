trigger OppLineItemtrigger on OpportunityLineItem (after insert,after delete,after update,before insert) 
{
     /*if(trigger.isafter && trigger.isinsert)
    {
        OpportunityLineItemHandler.creatingAssest(trigger.new);
    }
     if(trigger.isafter && trigger.isinsert)
    {
        OppLineItemHandler.creatingquotationWhenOpportunityLineItemIsInserted(trigger.new);
        //OppLineItemHandler.updatingTotalSaleQuantityWhenOppIsInserted(trigger.new);
        OppLineItemHandler.sendemail(trigger.new);
    }
    if(trigger.isafter && trigger.isdelete)
    {
        OppLineItemHandler.deletingOpportunityWhenopportunityLineitemIsDeleted(trigger.old);
    }
    
     if(trigger.isafter && trigger.isinsert)
     {
         //OppLineItemHandler.updatingProductQunatity(trigger.new);
         OppLineItemHandler.sendemail(trigger.new);
     }
    if(trigger.isbefore && trigger.isinsert)
    {
        OppLineItemHandler.generatingErrorWhenProducttypeIsdifferent(trigger.new);
    }*/
    

}